# AutoUI

AutoUI 面向不同类型的用户界面测试目标，提供一致的测试执行体验。各类测试目标拥有独立测试用例，不要求同一业务用例跨目标复用。

## Language

**测试目标（Test Target）**：
一次测试所操作的用户界面类型。当前规划中的测试目标包括桌面 Web、移动 Web、移动 App、桌面 App，以及尚未纳入近期范围的小程序。
_Avoid_: 平台、终端

**桌面 Web（Desktop Web）**：
运行在桌面操作系统浏览器中的 Web 用户界面，例如 Chrome 中的网站。
_Avoid_: PC 浏览器、Web 端

**移动 Web（Mobile Web）**：
运行在 Android 或 iOS 移动设备浏览器中的 Web 用户界面，例如 Safari、Chrome 或设备自带浏览器。
_Avoid_: H5、手机网页

**移动 App（Mobile App）**：
安装并运行在 Android 或 iOS 真机或模拟设备中的应用程序。
_Avoid_: App、移动端

**桌面 App（Desktop App）**：
安装并运行在桌面操作系统中的客户端应用程序。
_Avoid_: PC、PC 端

**小程序（Mini Program）**：
运行在宿主应用内部的小程序用户界面；当前仅为远期设想，具体宿主和执行环境尚未确定。
_Avoid_: 小程序端

**业务步骤（Business Step）**：
测试场景中对业务读者有意义的一段行为，用于说明场景正在完成什么；步骤数量由场景本身决定，不与底层 UI 操作或目标专属诊断证据一一对应。
_Avoid_: UI 操作、Playwright action、固定数量步骤

**业务操作（Business Operation）**：
测试场景可调用的一段可复用业务行为，由一个或多个目标交互共同完成；它描述要达成的业务意图，而不是具体点击方式。
_Avoid_: 页面操作、控件操作、业务方法

**公共结果摘要（Common Result Summary）**：
不同测试目标共同提供的执行结果概览，使使用者能够以一致方式识别运行、判断结果并找到诊断证据。
_Avoid_: 统一过程日志、统一诊断页面

**目标专属诊断证据（Target-specific Diagnostic Evidence）**：
由具体测试目标产生、并使用适合该目标的查看方式解释的诊断材料；不同目标不要求产生相同的详细过程记录。
_Avoid_: 通用 Trace、跨端统一步骤流

**业务报告（Business Report）**：
面向业务读者组织测试结果、业务步骤和失败信息的可读报告；它说明测试做了什么，不承担底层 UI 操作诊断。
_Avoid_: 技术操作日志、诊断 Trace

**性能观测（Performance Observation）**：
在测试执行期间采集的 CPU、内存、GPU 等时序数据；它独立于业务步骤和目标专属诊断证据，不要求与单个 UI 操作建立关联。
_Avoid_: Business Step 指标、Trace action 指标

**测试平台（Test Platform）**：
汇总不同测试目标的测试运行，并在统一使用体验中呈现业务报告、目标专属诊断证据和性能观测；各类证据由彼此隔离的查看方式解释。
_Avoid_: 测试执行框架、统一过程播放器

**测试结果（Test Outcome）**：
一次测试执行结束时形成的权威判定，例如通过、失败、跳过或错误；业务报告和测试平台呈现同一个测试结果。
_Avoid_: Allure 结果、Trace 结果

**结果清单（Result Manifest）**：
未来仅在测试平台需要关联现有标准产物无法表达的运行信息时，才可能引入的最小版本化摘要；它不是当前框架的必备输出，也不复制业务步骤或目标工具的详细事件流。
_Avoid_: 必备平台契约、事件日志、完整运行日志
