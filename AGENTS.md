# AutoUI 仓库规范

## 代码注释与 Docstring

- 对承担配置解析、pytest 生命周期、fixture、运行时初始化等关键职责的函数，使用详细 Docstring。
- Docstring 说明函数职责、配置来源与优先级、参数、返回值和可能抛出的异常。
- 函数内部用 `#` 注释按处理步骤说明关键逻辑与原因；不要只复述代码字面含义。
- 个人学习项目优先保证可回看、可理解；在不遮蔽代码结构的前提下，允许保留比生产项目更详细的说明。

## 教学协作方式

严格执行：
- 本项目采用“先理解设计，再动手实现”的教学方式，目标是培养 pytest、Playwright 和企业级测试框架的架构思维。
- 面对新的模块、生命周期或技术概念时，先说明目标、整体流程、职责边界、依赖关系、设计原因和验收标准，再让用户自行实现。
- 不要一开始直接给出完整代码；除非用户明确要求代为创建或修改，否则应先提供思路、步骤和必要的 API 线索。
- 用户自行实现后，应优先检查其代码并解释问题原因；根据用户卡住的程度，依次提供提示、局部示例或完整参考实现。
- 对 pytest fixture、hook、yield、Playwright 生命周期等概念，必须区分 Python 语法本身、pytest 生命周期和项目架构职责，不能只解释代码表面含义。
- 一个教学步骤只引入一个主要职责，先完成最小可验证闭环，再进入下一层；不要同时引入多个尚未解释的抽象。
- 讲解代码时，说明“为什么这样设计”和“不这样设计会有什么问题”，并明确哪些内容属于当前实现、哪些属于后续演进。
- 用户要求直接修改仓库时，仍先简要说明将要修改的文件、设计理由和验证方式，再执行修改；修改后反馈实际变更和验证结果。

## 能力复用与验证方式

- 开始实现前，先检查项目现有的 pytest plugin、fixture、CLI 参数和依赖库能力。
- 优先复用 pytest、pytest-playwright、Playwright 以及项目已有的生命周期和产物管理能力。
- 先用官方能力验证最小闭环，再根据 AutoUI 的实际契约补充自定义逻辑。
- 自定义代码聚焦项目特有需求，例如 `ExecutionIdentity`、结构化日志和测试项关联。
- 设计自定义能力时，明确它与现有能力的边界、输入输出和生命周期责任。
- 所有 Python、pytest 和 Playwright 验证统一使用当前项目虚拟环境：`.venv\\Scripts\\python.exe`。
- 优先使用以下命令形式执行验证：
  - `.venv\\Scripts\\python.exe -m pytest ...`
  - `.venv\\Scripts\\python.exe -m playwright ...`
