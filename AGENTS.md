# 教学与实现规范

## 规则
- 关于 safety：
	1. 禁止 safety 与 security 相关的审计
	2. 禁止引入非常规安全设计和校验
	3. 禁止在没有明确攻击者和攻击模式的情况下做 safety
- 关于设计：
	- 不用兼容旧实现
	- 可以适当调整目录结构或内容结构

## 教学原则

本项目采用 **“先理解设计，再动手实现”** 的教学方式，重点培养 pytest、Playwright 和企业级测试框架的架构思维。

讲解任何新增设计或实现时：

1. 先说明它解决的问题、职责边界以及在整体架构中的位置。
2. 说明 **为什么这样设计**，以及不这样设计可能产生的问题。
3. 说明相关依赖、数据流和调用关系，避免只解释局部代码。
4. 明确区分：
   - 当前步骤需要完成的内容；
   - 暂不处理、属于后续演进的内容。
5. 一次只引入一个主要职责，复杂功能先完成最小闭环，再逐层扩展。
6. 实现前优先查看项目中已有的相关代码和约定，保持架构、命名和实现风格一致；已有代码仅作为设计参考，不机械照搬。

例如：
`事件对象 → 输出机制 → pytest fixture → Page Object → 测试用例`

测试用例只负责业务验证，不重复承担框架基础设施职责。

## 方法设计与实现

新增类、函数或方法时，遵循 **“先设计、后实现”**：

1. **职责与位置**：说明负责什么，以及与其他组件的关系。
2. **参数与依赖**：说明每个参数的：
   - 业务含义和类型；
   - 来源；
   - 是否必填及默认值；
   - 约束；
   - 最终如何被使用、转换、保存、传递或记录。
3. **调用与数据流**：说明谁调用它、依赖如何进入、结果流向哪里。
4. **设计理由**：解释当前设计及替代设计可能带来的问题。
5. **实现边界**：明确当前实现和后续演进内容。
6. **验收标准**：说明当前步骤满足什么条件即可完成。
7. **参考实现**：最后提供完整代码。

## 代码与 Docstring

给出的代码应遵循项目现有风格，并包含必要的 Docstring 和关键逻辑注释。

Docstring 应根据实际情况说明：
- 职责；
- 配置或依赖来源及优先级；
- 参数；
- 返回值；
- 可能抛出的异常。

注释优先解释 **设计原因、生命周期、依赖关系、数据流和异常处理**，可以参考本仓库内其他代码的注释结构

## 能力复用与验证方式

- 开始实现前，先检查项目现有的 pytest plugin、fixture、CLI 参数和依赖库能力。
- 优先复用 pytest、pytest-playwright、Playwright 以及项目已有的生命周期和产物管理能力。
- 先用官方能力验证最小闭环，再根据 AutoUI 的实际契约补充自定义逻辑。
- 所有 Python、pytest 和 Playwright 验证统一使用当前项目虚拟环境：`.venv\\Scripts\\python.exe`。
- 优先使用以下命令形式执行验证：
  - `.venv\\Scripts\\python.exe -m pytest ...`
  - `.venv\\Scripts\\python.exe -m playwright ...`
