---
类型: 主题笔记
创建时间: 2026-08-02
标签:
  - 项目计划
  - 测试开发
  - Python
  - Pytest
  - Playwright
  - CI
适用场景: 用于推进 AutoUI 企业可用 Web 自动化框架 v1，跟踪项目范围、阶段交付、设计约束和验收门禁。
---

# 2026 年 9 月项目计划 - AutoUI 企业可用框架 v1

> [!summary] 项目目标
> 在 9 月完成一套可重复执行、可并行、可诊断、可接入 CI 的 Web UI 自动化框架 v1。
>
> 项目不追求目录和抽象数量，而以真实回归流程、稳定生命周期、失败诊断和 CI 门禁作为完成标准。

---

## 项目背景与基线

AutoUI 当前已具备 pytest-playwright、基础 Page Object、YAML 参数化和 Context 配置等基础能力。

本项目以现有代表流程作为回归安全网，在保留 Playwright 原生 Locator、auto-waiting、`expect()` 以及 pytest-playwright 原生产物能力的前提下完成框架重构。

---

## 项目范围

### 本月交付

```text
模块化 pytest 框架骨架。
配置优先级、启动校验与 secrets 边界。
独立 BrowserContext、运行身份和并行隔离。
结构化日志、失败截图和 trace。
JUnit XML、Allure results 与 GitHub Actions 门禁。
串行、xdist -n 2、受控失败和 CI 验收记录。
```

### 本月不做

```text
不接入 Appium、移动端和 PC 自动化实现。
不创建 CommonDriver、BaseTestCase、Locator 包装层或二次等待层。
不建设测试平台、设备农场、账号池和复杂重试机制。
不为了未来多端预先创建 app/、pc/ 或空的抽象接口。
```

---

## 工程设计原则

### 真实需求驱动的文件与抽象

```text
不按“一个概念一个文件”拆分代码。

新增 .py 文件必须满足至少一项：
1. 它有独立职责，且与现有文件的依赖不同。
2. 它被两个或以上真实调用方复用。
3. 留在当前文件会导致 pytest、Playwright、配置或运行时职责明显混杂。

不因未来可能性创建空模块、空目录、空基类或空 Protocol。
当真实调用方出现后，再提炼 Component、Flow、runtime、evidence 等模块。
```

当前配置阶段只保留：

```text
autoui/core/config/settings.py
  配置读取、优先级解析、校验、Settings 构造。

autoui/plugins/options.py
  pytest CLI 参数、session 初始化和 settings fixture。
```

### 多端演进边界

```text
当前仅实现 Web。

未来 App / PC 可共享配置、运行身份、日志、证据和报告契约；
不提前统一 click、fill、Locator、等待、断言或 Driver。

只有多个平台出现经过验证的共同需求时，才提炼跨端契约。
```

### 依赖方向

```text
tests -> Web Pages / Flows -> pytest fixtures -> pytest-playwright
pytest plugins -> core config / runtime
core 不依赖 pytest、Playwright 或具体平台实现
```

---

## 阶段 1：模块骨架、配置与生命周期

目标：

```text
完成清晰的 Web 框架包结构。
实现 CLI > YAML 配置的运行配置优先级。
将账号、密码、token 限制为环境变量或 GitHub Secrets。
在 Browser 启动前校验必填配置。
保留一个可串行执行的代表回归流程。
```

验收：

```text
pytest --collect-only -q --strict-markers 成功。
缺少必填配置时，在 Browser 启动前清晰失败。
配置完整时，代表流程串行通过。
运行凭据不进入仓库配置、YAML 或 Settings；
内部测试日志与报告允许按诊断需求记录明文凭据、输入数据和错误上下文。
```

---

## 阶段 2：BrowserContext、运行身份、原生产物与并行

目标：

```text
每个 pytest item 使用独立 BrowserContext。
建立 testrun_uid、worker_id 和 test identity。
建立 AutoUI 的运行身份，并确认 pytest-playwright 原生输出目录能够隔离每个测试项。
不重复实现 pytest-playwright 已提供的截图、trace 和产物目录管理。
使用 pytest-xdist -n 2 证明至少两个 worker 实际执行测试。
```

验收：

```text
串行与 -n 2 的结果一致。
不同测试不覆盖数据、日志、截图或 trace；产物目录由 pytest-playwright 的测试项输出路径隔离。
连续执行不读取上次残留状态。
```

---

## 阶段 3：失败证据、日志、报告与 CI

目标：

```text
统一关联结构化日志、截图、trace 与 pytest report。
使用 pytest-playwright 的 `--tracing`、`--screenshot` 和 `--output` 配置生成浏览器证据。
使用 nb_log 作为日志基础设施，通过自定义 JSON formatter 输出 JSONL。
通过日志 fixture 将 ExecutionIdentity、pytest item 和原生 `output_path` 绑定起来。
每个测试项独立生成 `autoui-events.jsonl` 与 `autoui-errors.jsonl`，不使用跨 worker 的全局日志文件。
保留完整的内部测试执行信息，包括操作参数、输入数据和错误上下文。
只有存在真实的自定义日志或元数据写入需求时，才通过原生 `output_path` 写入对应测试目录。
生成 JUnit XML 与 Allure results。
建立 GitHub Actions，在干净环境安装依赖、浏览器并执行并行回归。
失败时仍上传证据，但不掩盖测试失败退出码。
```

验收：

```text
CI 收集错误、未知 marker、安装失败和回归失败都会失败。
失败后可按测试项和输出目录找到完整证据。
结构化日志包含统一 schema、时间、日志级别、事件类型、测试身份和事件数据。
操作日志首先记录 Playwright 底层操作的生命周期、目标、参数及操作结果；错误日志记录异常类型、消息、堆栈和关联产物。
业务语义不在底层操作日志中推断，后续通过独立的 business step 装饰器或步骤层补充。
同一测试项的 JSONL、截图、trace、JUnit 和 Allure 结果可以通过 nodeid 与 output_path 关联。
JUnit、Allure results 与 CI artifacts 可下载和查看。
```

---

## 月度完成标准

```text
代表回归流程可稳定串行运行。
xdist -n 2 实际由两个 worker 执行，且结果一致。
测试具备独立 Context、数据和 artifact。
配置有明确优先级，运行凭据不进入仓库配置文件；内部测试日志与报告允许按诊断需求保留包含明文凭据在内的完整执行信息。
失败后可定位日志、截图和 trace。
GitHub Actions 能阻止不合格变更。
```

---

## 架构决策记录

```text
ADR-001：当前仅实现 Web，多端只保留演进边界。
ADR-002：非敏感运行配置采用 CLI > YAML。
ADR-003：secrets 仅从环境变量和 GitHub Secrets 注入，不进入仓库配置、YAML 或 Settings；内部测试日志与报告允许记录明文值。
ADR-004：pytest plugin 生命周期替代 BaseTestCase 与 CommonDriver。
ADR-005：文件与抽象只由真实职责和真实调用方驱动。
ADR-006：浏览器证据统一采用 pytest-playwright 原生能力，AutoUI 通过 `output_path` 扩展自定义日志和元数据。
ADR-007：nb_log 作为日志基础设施，AutoUI 通过结构化日志 fixture 注入运行身份和测试项输出目录。
ADR-008：结构化日志采用 JSONL；每个测试项独立生成操作日志与错误日志，错误事件可同时保留在完整事件日志中。
```

## 当前运行时设计

### 能力归属

```text
pytest-playwright
  负责 Browser、BrowserContext、Page 的生命周期。
  负责 trace、screenshot、video 的采集开关与测试项输出目录。
  通过 --tracing、--screenshot、--output 等原生参数控制行为。

autoui.plugins.options
  负责 AutoUI 自己的 CLI 参数、配置合并和 Settings 初始化。

autoui.plugins.web
  负责将 Settings 转换为 browser_context_args。
  负责提供 AutoUI 的 Page Object fixture。
  不读取 YAML，不管理 trace，不保存 screenshot。

autoui.plugins.runtime
  负责构造 ExecutionIdentity：testrun_uid、worker_id、nodeid。
  提供 autoui_logger fixture，将 ExecutionIdentity 与 pytest-playwright 提供的 output_path 组合起来。

autoui.plugins.playwright_logging
  负责在 pytest 生命周期中绑定当前 AutoUILogger，
  并包装 Page、Locator 的公共动作，自动记录底层 Playwright 操作。

autoui.core.logging
  负责 AutoUI 事件 schema、结构化事件封装和 JSONL writer。
  不依赖 pytest、Playwright 或具体 Page Object。

nb_log
  负责 logger、文件 handler、错误日志分流和自定义 JSON formatter 的底层输出。
  不负责理解 pytest item、ExecutionIdentity 或 AutoUI 业务事件。
```

### 产物策略

```text
CI / 本地命令
  --output=artifacts
  --tracing=retain-on-failure
  --screenshot=only-on-failure
          |
          v
pytest-playwright
  按测试项生成隔离输出目录，并在测试结束时保留对应证据
          |
          v
artifacts/
  <test-item-output>/
    trace.zip
    test-failed-1.png
    autoui-events.jsonl
    autoui-errors.jsonl
```

trace 提供操作时间线、页面状态和网络等诊断信息，是失败时的深度浏览器诊断证据。
结构化日志记录底层 Playwright 操作的生命周期、目标、参数和结果，
作为企业平台的主要操作过程查看入口，不负责推断业务语义。
后续业务语义通过独立的 business step 装饰器或步骤层补充，
不在底层操作日志中重复实现业务逻辑。
后续新增证据类型时，必须由明确的诊断需求驱动，并写入对应测试项的输出目录。


## 第 2 周进度记录（2026-08-10）

### 已完成

- 确认采用“多端预留边界、当前只实现 Web”的架构方向。
- 明确跨端共享 config、runtime、logging、evidence、报告等运行框架能力；Web/App/PC 的 UI 操作、Locator、Driver 不提前强行统一。
- 完成 AutoUI 目录迁移的第一版：
  - `autoui/core/config/`
  - `autoui/plugins/`
  - `autoui/platforms/web/pages/`
  - `tests/web/regression/`
  - `tests/web/data/`
- 根 `conftest.py` 已改为注册新 plugin。
- 完成 strict collection 验证：`2 tests collected`。
- `settings.py` 已完成：
  - `load_profile(environment)`：从同级 `environments/{environment}.yaml` 读取 YAML。
  - `build_settings(profile)`：将 profile 字典转换为不可变 `Settings`、`Viewport` 对象。
- 已理解 `__file__`、`Path.resolve()`、`Path.parent`、路径拼接 `/` 与 `Path.open()` 的作用。

### 当前设计约束

- 不再为了“企业框架”预先创建大量空 Python 文件。
- 当前配置实现只保留两层：
  - `autoui/core/config/settings.py`：YAML、CLI 配置解析、Settings 构建与校验；不依赖 pytest。
  - `autoui/plugins/options.py`：pytest CLI 参数、session hook、settings fixture；不再新增 `configuration.py`。
- `web.py` 只消费 `settings` fixture，不自行读取 YAML、环境变量或 CLI。
- 运行凭据不进入 YAML、Settings 和版本库；内部测试日志与报告按诊断需求保留完整执行信息，允许包含明文凭据和输入数据。

### 阶段 1 配置与生命周期进度（2026-08-12）

#### 已完成

- `conftest.py` 已注册 `autoui.plugins.options`，pytest 能发现 AutoUI 的 hook 与 fixture。
- `options.py` 已注册 AutoUI 参数：
  - `--site`：`cn`、`com`、`zawa`。
  - `--env`：`pre`、`beta`、`release`。
  - `--base-url` 复用 pytest-playwright 原生参数，不重复注册。
- 环境配置采用两个真实维度：站点 × 环境。
  - `environments/defaults.yaml`：默认站点、默认环境和公共浏览器配置。
  - `environments/targets.yaml`：每个 `site / environment` 组合的 `base_url`。
- `resolve_settings()` 已将配置来源合并为一个不可变 `Settings`：

```text
defaults.yaml 的公共字段
+ targets.yaml[site][environment] 的目标地址
+ CLI 覆盖
-> Settings
```

- 当前优先级：

```text
site：--site > defaults.yaml
environment：--env > defaults.yaml
base_url：--base-url > targets.yaml
```

- `pytest_sessionstart()` 在浏览器启动前只构造一次 `Settings`，保存至 `config.autoui_settings`；`settings` fixture 将同一份配置交给 `web.py` 的 `browser_context_args`。
- 已验证：默认配置、CLI 选择站点/环境、CLI 临时 URL 覆盖、未知站点/环境报错、缺少必填字段报错。
- 代表回归流程已串行通过；`pytest -n 2` 已确认两个 worker 实际执行参数化用例。

#### 配置“合并”与“分离”的边界

```text
配置值：在 resolve_settings() 内合并，最终只向下游提供一个 Settings。
配置来源：defaults.yaml 与 targets.yaml 分离，避免公共字段在 9 个站点/环境组合中重复。
配置消费：通过 pytest fixture 分离，web.py 不重复读取 CLI 或 YAML。
```

这三层并不冲突：分离来源与职责，是为了保证最终生成的 `Settings` 唯一且一致。

### 第 3 周进度记录（2026-08-15）

#### 阶段 1：已完成

- 非敏感运行配置已收敛为 `CLI > YAML`；不使用环境变量覆盖站点、环境或 URL。
- `settings.py` 的核心职责为读取 YAML、合并 CLI 覆盖、校验并构造不可变 `Settings`。
- `pytest_sessionstart()` 只在 session 启动时构造一次 `Settings`，保存至 `config.autoui_settings`。
- `settings` fixture 通过 `request.config.autoui_settings` 向下游提供同一份配置；`web.py` 只声明 `settings` 依赖，不感知 CLI、YAML 与 Config 的存储细节。
- 环境变量和 GitHub Secrets 仍用于注入账号、密码、token、API key 等运行凭据；这些值允许以明文形式作为完整测试执行信息写入内部日志与报告。

#### 阶段 2：已完成的基础能力

- 已确认 pytest-playwright 的 `context` fixture 为函数级别：每个 pytest item 创建独立 BrowserContext，并在测试结束后关闭。
- 代表回归流程已串行通过。
- 使用 `pytest -n 2 -vv` 验证两个 worker 实际执行参数化用例，串行与并行结果一致。

#### 阶段 2：运行身份与产物边界已完成

1. 已建立由 `testrun_uid`、`worker_id`、`nodeid` 组成的 `ExecutionIdentity`。
2. 已确认 pytest-playwright 原生提供 `--output`、`output_path`、`--tracing` 和 `--screenshot`，AutoUI 的浏览器证据统一遵循该能力边界。
3. 已完成运行时插件和 Web 插件的职责收敛：runtime 提供执行身份，Web 提供配置转换和页面对象。
4. 已完成 `ExecutionIdentity` 单元测试与 pytest fixture 契约测试，当前定向验证结果为 `3 passed`。

#### 当前下一步

1. 使用 pytest-playwright 原生参数验证失败截图、trace 和测试项输出目录。
2. 根据真实日志需求决定是否使用 `output_path` 写入结构化日志。
3. 设计结构化日志与 JUnit / Allure 报告的关联方式。

#### 计划边界

- 当前不额外增加 `resolve_settings()` 和 `build_settings()` 的独立单元测试，优先推进结构化日志和主链路验收。

### 第 3 周进度记录（2026-08-16）

#### 阶段 2：已完成

- 已将默认 pytest 运行模式调整为 headless，移除 `pyproject.toml` 中的 `--headed`，避免 CI 在无显示环境下启动失败。
- 已使用 pytest-playwright 原生参数完成失败证据验证：
  - `--output=artifacts\phase2-evidence`
  - `--tracing=retain-on-failure`
  - `--screenshot=only-on-failure`
- 已确认不同测试项生成独立的输出目录，并在失败时生成 `test-failed-1.png` 与 `trace.zip`。
- 已完成串行与 `pytest -n 2` 并行回归验证，两个 worker 均能实际执行测试，结果一致。
- 已确认 AutoUI 不重复实现截图、trace 或 BrowserContext 生命周期管理，继续遵循 pytest-playwright 原生能力边界。
- 已将 `artifacts/` 和 `.pytest_cache/` 加入 `.gitignore`，避免测试产物和本地缓存进入版本库。

#### 当前进入阶段 3：失败证据、日志、报告与 CI

1. 已使用 `AutoUILogger` 作为 AutoUI 统一事件入口，并以 `nb_log` 作为底层 logger；自定义 JSON formatter 尚待完成。
2. 使用 pytest-playwright 的 `output_path` 将每个测试项的结构化日志写入对应测试目录；JSONL 是企业平台查看操作过程的主要入口。
3. 通过 `autoui_logger` fixture 自动绑定 `testrun_uid`、`worker_id`、`nodeid`，并管理当前测试项的日志生命周期。
4. 完成 `AutoUILogger.emit()` 和 `operation()`，统一记录 `started`、`finished`、`failed` 事件，并通过 `operation_id` 关联同一次操作。
5. 在 pytest 插件层包装 Playwright 的 `Page` 和 `Locator` 公共动作，自动记录 `goto`、`fill`、`click` 等底层操作；不要求 Page Object 或测试用例手动写日志。
6. 保留 pytest-playwright 原生 Trace 作为失败时的深度诊断证据，不把 Trace 作为日常业务查看入口。
7. 后续再通过独立的 business step 装饰器或步骤层补充“搜索订单”等业务语义，不修改底层操作日志的职责。
8. 待验证结构化日志与 JUnit、Allure、截图、trace 是否能通过 nodeid、operation_id 和 output_path 关联。

#### 计划边界

- 当前不额外增加 `resolve_settings()` 和 `build_settings()` 的独立单元测试。

### 第 4 周进度记录（2026-08-23）

#### 阶段 3：底层操作日志采集链路已完成

- 已创建 `autoui_logger` autouse fixture，为每个测试项绑定独立的 `AutoUILogger`、`ExecutionIdentity` 和 `output_path`。
- 已通过 pytest 插件包装 `Page.goto`、`Locator.fill` 和 `Locator.click`，测试用例与 Page Object 不需要手动编写底层操作日志。
- 已完成 `started`、`finished`、`failed` 生命周期事件和 `operation_id` 关联。
- 已使用百度搜索 smoke 用例验证底层操作会生成 `autoui-events.jsonl` 和 `autoui-errors.jsonl` 文件。
- 已保留 pytest-playwright 原生截图和 Trace，作为失败时的深度诊断证据。

#### 阶段 3：尚未完成

- 当前 `.jsonl` 文件仍使用 `nb_log` 默认文本 formatter，并非真正的 JSONL。
- 测试断言失败尚未统一写入错误日志；当前错误日志主要依赖被包装 Playwright 动作抛出的异常。
- 尚未完成 JSONL、JUnit、Allure、截图和 Trace 的统一关联验收。

#### 当前下一步

1. 实现并接入自定义 JSON formatter，使每行日志都成为可解析的 JSON 对象。
2. 验证日志 schema、操作生命周期和错误事件。
3. 再补充测试级失败事件，并验收日志与 JUnit、Allure、截图和 Trace 的关联。
