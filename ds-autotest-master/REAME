推演自动测试项目

一、怎么判断一个推演是否成功？
通过successCondition设置来指定
successCondition的类型为SuccessCondition，有三个属性：
1、status 可选参数，如果不设置，系统会自动补充为RUN_FINISHED
指的是推演结束时的状态，例如：NOT_SUPPORTED、RUN_FINISHED、STOPPED。。。

2、result 可选参数，数据类型为string或者string[]
指的是推演过程中通过setDsResult写入的值，要在推演结束后取出来跟这个result比较，比较方式间resultMatchType，匹配成功则判定推演成功

3、resultMatchType，取值有：equals、regex和substring
这是个可选参数。在result存在而resultMatchType不存在的情况下，系统会自动补全resultMatchType为equals

所以判断推演是否成功的方法为：
1、如果推演结束状态不等于successCondition.status，那么失败；
2、否则，
  2.1、如果没有设置result，那么成功
  2.2、获取推演结果，跟result比较，匹配则成功，否则失败

匹配成功的含义：
result为string时可以看成长度为1的string[]，所以我们把result当数组看

当resultMatchType为：
1、equals
推演结果跟result数组中任一项相等，则认为匹配成功

2、regex
result数组中为正则表达式，这些正则表达式任一个匹配了推演结果，则认为匹配成功
注意：别搞反了

3、substring
result数组中任一项是推演结果的子串，则认为匹配成功
注意：别搞反了


二、程序使用方式
1、拉ds-autotest代码
2、vscode打开ds-autotest
3、安装依赖包
这里要注意，依赖包有两个npm源：
http://192.168.1.130:8081/repository/npm-group/
http://192.168.1.130:8081/repository/npm-hosted/
因为第一个只是个镜像，不能发布包，而这个测试项目用了我发布的一个包，发布在了第二个源上

所以安装的时候，要切换源，分别执行npm i
切换源方式：npm config set registry=xxx

4、检查修改你要使用的推演服务的地址
在utils\request.ts文件中，第8行
baseURL: 'http://127.0.0.1:8180',
修改这个地址就可以了

5、npm run build
成功之后就可以测试了

6、拉模型，假设模型存储路径为d:\test-models

7、执行测试
在vscode的终端窗口中输入
npm run start testDir=d:\test-models
